<%= form_for(@department) do |f| %>
  <% if @department.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@department.errors.count, "error") %> prohibited this department from being saved:</h2>

      <ul>
      <% @department.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <!-- <div class="field">
 	 <%= f.collection_select(:customer_id, @customers, :id, :name, {prompt: "Select a Customer"}) %> <br />
  </div> -->
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :valid_key %><br />
    <%= f.text_field :valid_key %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
